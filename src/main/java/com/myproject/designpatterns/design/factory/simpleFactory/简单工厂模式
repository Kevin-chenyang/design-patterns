简单工厂模式：
工厂类：面向抽象类的创建方法
抽象产品：抽象方法
具体产品：具体实现方法



比如现在有客户需要使用手机语音只能
首先需要通过通过工厂类创建一个手机出来，
至于手机自己内部是使用的siri，还是小艺，（这一部分就是抽象成一个抽象产品，工厂类就面向这个抽象产品类去实现创建工作），
因此这一部分代码就是变化的部分，这个就可以放在具体的产品类内部去实现

优点：对扩展开放、对修改关闭
缺点：每增加一种产品都需要在工厂类逻辑中修改判断逻辑，又违背了开闭原则
适用于业务简单的情况下或者具体产品很少增加的情况。而对于复杂的业务环境可能不太适应了