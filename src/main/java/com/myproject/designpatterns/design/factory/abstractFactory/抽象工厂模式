
抽象工厂类（针对共性的特征进行抽取）
具体的工厂类
抽象的产品
具体的产品（作为父类）+产品的特征共性的一类产品（子类）


抽象工厂模式中我们可以定义实现不止一个接口，一个工厂也可以生成不止一个产品类，
抽象工厂模式较好的实现了“开放-封闭”原则，是三个模式中较为抽象，并具一般性的模式


简单来说：工厂方法模式的工厂是创建出一种产品，而抽象工厂是创建出一类产品。

比如说现在手机千变万化，有折叠的，有5G的，有曲面平的，而面对这类产品之前的创建模式就不适用了，
这种概念就不是一个产品了，而是一类产品，这个就是抽象工厂模式



spring里的应用：Connection
